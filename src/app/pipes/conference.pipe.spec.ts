import { ConferencePipe } from './conference.pipe';

describe('ConferencePipe', () => {
  it('create an instance', () => {
    const pipe = new ConferencePipe();
    expect(pipe).toBeTruthy();
  });
});
